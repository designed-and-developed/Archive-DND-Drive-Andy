type User {
  id: Int!
  username: String!
  password: String!
}

type Mutation {
  createUser(username: String!, password: String!): User!

}

type Query {
  users: [User]!
  user(id: Int!): User
}

